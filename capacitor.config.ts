import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app_rick_and_morty',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
